#include <stdbool.h>

#define ON        '1'
#define OFF       '0'
#define WORD_SIZE 16

// TODO: Name the params (documentation)
bool streq(char *, char *);
bool strin(char *, char);
void strrev(char *);
void itobin(int, char *, int);

