#include <stdio.h>

int main()
{
    // For MVP:
    // 1. Parse input text into tokens
    // 2. Parse tokens into expressions or errors
    // 3. Translate expressions into bin code
    return 0;
}
